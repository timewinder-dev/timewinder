[spec]
expected_error = "Termination"

[threads.light]
entrypoint = "light_process()"

[threads.car]
entrypoint = "car_process()"

[properties]
# Car eventually drives away (termination condition)
#CarDrives = { Eventually = "not at_light" }
