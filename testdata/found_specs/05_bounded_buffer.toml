[spec]
file = "05_bounded_buffer.star"

[threads.producer]
entrypoint = "producer()"
fair = true

[threads.consumer]
entrypoint = "consumer()"
fair = true

[properties]
BufferBounded = {Always = "len(buf) <= 3"}      # Buffer never exceeds size limit
